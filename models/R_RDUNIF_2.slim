/*
	This exercises rdunif() for the singleton non-coin-flip case
*/

initialize() { }
1 early() {
	start = clock("mono");
	for (i in 1:400)
		x = rdunif(10000000, 0, 2);
	catn("// ********** Benchmark time: " + (clock("mono") - start));
}
