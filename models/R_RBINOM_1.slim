/*
	This exercises rbinom() for the singleton coin-flip case
*/

initialize() { }
1 early() {
	start = clock("mono");
	for (i in 1:1000)
		x = rbinom(10000000, 1, 0.5);
	catn("// ********** Benchmark time: " + (clock("mono") - start));

	// summarize results
	catn("### NO SUMMARY ###");
}
