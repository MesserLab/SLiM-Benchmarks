/*
	This exercises rdunif() for the singleton coin-flip case
*/

initialize() { }
1 early() {
	start = clock("mono");
	for (i in 1:1000)
		x = rdunif(10000000);
	catn("// ********** Benchmark time: " + (clock("mono") - start));

	// summarize results
	catn("### NO SUMMARY ###");
}
