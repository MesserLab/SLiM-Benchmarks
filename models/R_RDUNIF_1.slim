/*
	This exercises rdunif() for the singleton coin-flip case
*/

initialize() { }
1 early() {
	start = clock("mono");
	for (i in 1:500)
		x = rdunif(10000000);
	catn("TEST TIME: " + (clock("mono") - start));
}
