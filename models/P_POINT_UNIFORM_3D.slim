/*
	This exercises the Subpopulation method pointUniform()
*/

initialize() {
	initializeSLiMOptions(dimensionality="xyz");
}
1 late() {
	sim.addSubpop("p1", 100);
	p1.setSpatialBounds(c(0, 0, 0, 100, 100, 100));
	
	start = clock("mono");
	for (i in 1:100) p1.pointUniform(10000000);
	catn("// ********** Benchmark time: " + (clock("mono") - start));

	// summarize results
	catn("### NO SUMMARY ###");
}
