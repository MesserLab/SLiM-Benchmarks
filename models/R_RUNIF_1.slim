/*
	This exercises runif() for the singleton 0.0-1.0 case
*/

initialize() { }
1 early() {
	start = clock("mono");
	for (i in 1:250)
		x = runif(10000000);
	catn("// ********** Benchmark time: " + (clock("mono") - start));

	// summarize results
	catn("### NO SUMMARY ###");
}
