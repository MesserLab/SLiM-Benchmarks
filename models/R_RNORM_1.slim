/*
	This exercises rnorm() for the sigma & mu singleton case
*/

initialize() { }
1 early() {
	start = clock("mono");
	for (i in 1:30)
		x = rnorm(10000000, 10.0, 5.0);
	catn("// ********** Benchmark time: " + (clock("mono") - start));
}
