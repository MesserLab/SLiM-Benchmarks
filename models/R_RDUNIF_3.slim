/*
	This exercises rdunif() for the no singleton case
*/

initialize() { }
1 early() {
	min = rdunif(10000000, 0, 10);
	max = rdunif(10000000, 20, 50);
	
	start = clock("mono");
	for (i in 1:150)
		x = rdunif(10000000, min, max);
	catn("// ********** Benchmark time: " + (clock("mono") - start));

	// summarize results
	catn("### NO SUMMARY ###");
}
