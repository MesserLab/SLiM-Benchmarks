/*
	This exercises dnorm() for the sigma & mu singleton case
*/

initialize() { }
1 early() {
	x = runif(10000000);
	
	start = clock("mono");
	for (i in 1:150)
		y = dnorm(x, 0.5, 1.5);
	catn("// ********** Benchmark time: " + (clock("mono") - start));
}
