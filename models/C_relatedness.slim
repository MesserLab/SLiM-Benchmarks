/*
	C_related.slim
	
	This exercises the Individual method relatedness()
*/

initialize() {
	initializeSLiMOptions(keepPedigrees=T);
}
1 early() { sim.addSubpop("p1", 100); }
19 late() {
	p1.setSubpopulationSize(1000000);
}
20 late() {
	inds = p1.individuals;
	focalInd = inds[0];
	
	start = clock("mono");
	for (i in 1:150) focalInd.relatedness(inds);
	catn("TEST TIME: " + (clock("mono") - start));
	
	// summarize results
	catn("### NO SUMMARY ###");
}
