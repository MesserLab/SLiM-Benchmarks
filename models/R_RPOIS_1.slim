/*
	This exercises rpois() for the lambda singleton case
*/

initialize() { }
1 early() {
	start = clock("mono");
	for (i in 1:10)
		x = rpois(10000000, 10.5);
	catn("TEST TIME: " + (clock("mono") - start));
}
