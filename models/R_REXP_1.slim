/*
	This exercises rexp() for the mu singleton case
*/

initialize() { }
1 early() {
	start = clock("mono");
	for (i in 1:50)
		x = rexp(10000000, 10.5);
	catn("// ********** Benchmark time: " + (clock("mono") - start));
}
