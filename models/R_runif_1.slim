/*
	This exercises runif() for the singleton 0.0-1.0 case
*/

initialize() { }
1 early() {
	start = clock("mono");
	for (i in 1:500)
		x = runif(10000000);
	catn("TEST TIME: " + (clock("mono") - start));
}
