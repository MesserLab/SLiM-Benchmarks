/*
	C_setSpatialPosition_2.slim
	
	This exercises the Individual method setSpatialPosition(),
	setting a different point on each individual.
*/

initialize() {
	initializeSLiMOptions(dimensionality="xy");
}
1 late() {
	sim.addSubpop("p1", 1000000);
	p1.setSpatialBounds(c(0, 0, 100, 100));
	points = p1.pointUniform(p1.individualCount);
	inds = p1.individuals;
	
	start = clock();
	for (i in 1:500) inds.setSpatialPosition(points);
	catn("time for parallelizable fraction: " + (clock() - start));
}
