/*
	This exercises survival evaluation, using very slow turnover
	of a population with overlapping generations and with
	no genetics.
*/

initialize() {
	// This model uses an internal benchmark
	_startBenchmark("SURVIVAL");
	
	initializeSLiMModelType("nonWF");
	defineConstant("K", 10000000);
}
2 reproduction() {
	// make one offspring and stop
	subpop.addCloned(individual);
	self.active = 0;
}
1 early() {
	sim.addSubpop("p1", K);
}
early() {
	p1.fitnessScaling = 0.99999999;
}
100 late() {
	//catn("elapsed: " + _stopBenchmark());

	// summarize results
	catn("### NO SUMMARY ###");
}
